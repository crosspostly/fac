# Модуль Insta-Tok (Кросспостинг)

Этот модуль отвечает за автоматическую публикацию видео в **TikTok** и **Instagram** после их успешной загрузки на Rutube.

## 📂 Структура

*   `sync_insta_tok.py` — Автономный скрипт синхронизации (YouTube -> TikTok/Instagram).
*   `scheduler.py` — Планировщик (запуск каждые 3 часа).
*   `publish.py` — Утилита для ручной публикации одного видео.
*   `.env` — Конфигурация (логины/пароли).
*   `tiktok_cookies.txt` — Файл куков для авторизации в TikTok.
*   `venv/` — Виртуальное окружение с `instagrapi` и `tiktok-uploader`.

## ⚙️ Настройка

### 1. Instagram
В файле `.env` должны быть указаны учетные данные:
```env
INSTA_USERNAME=ваш_логин
INSTA_PASSWORD=ваш_пароль
```

**⚠️ Важно:** Instagram часто блокирует вход с дата-центров (IP серверов) или требует подтверждения (Challenge).
Если вы видите ошибку `Login FAILED... added to the blacklist`, попробуйте:
1.  Зайти в аккаунт через браузер с того же IP (если возможно, через VNC/прокси).
2.  Использовать прокси в настройках `instagrapi` (требует доработки кода).
3.  Использовать session-файл (дамп успешной сессии с локального ПК).

### 2. TikTok
TikTok требует файл куков в формате Netscape.
1.  Установите расширение "Get cookies.txt LOCALLY" для Chrome/Firefox.
2.  Зайдите на tiktok.com и залогиньтесь.
3.  Скачайте куки и сохраните как `tiktok_cookies.txt` в папку `rutube/insta_tok/`.

## 🚀 Интеграция с Rutube

Логика кросспостинга встроена в основной пайплайн Rutube (`../sync_production.py`).
Через модуль `../social_uploader.py`:
1.  Видео скачивается и грузится на Rutube.
2.  Если успешно -> вызывается `process_social_uploads`.
3.  Происходит загрузка в TikTok (сейчас **PAUSED** в коде).
4.  Происходит загрузка в Instagram.
5.  Файл удаляется.

## 🛠 Ручная проверка

Чтобы проверить логин в Instagram без загрузки видео:
```bash
../rutube/venv/bin/python ../rutube/test_insta_login.py
```
